# BTC TimesFM Dashboard Frontend

Vite + React + TypeScript dashboard for local BTC forecasting and paper backtesting.

## Setup

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8000` by default. Override with:

```bash
VITE_API_BASE_URL=http://localhost:8000 npm run dev
```

